spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/student_job_recommendation
    username: postgres
    password: "123456"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    show-sql: true
  flyway:
    enabled: true
